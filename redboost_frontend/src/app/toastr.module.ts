/*import { NgModule } from '@angular/core';
import { BrowserAnimationsModule } from '@angular/platform-browser/animations';
import { ToastrModule } from 'ngx-toastr';

@NgModule({
  imports: [BrowserAnimationsModule, ToastrModule.forRoot()], // Configuration globale de Toastr
  exports: [ToastrModule] // Exporter ToastrModule pour qu’il soit disponible dans d’autres composants
})
export class ToastrSharedModule {}*/
